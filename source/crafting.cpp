#include "engine.hpp"

class Crafting {

	std::vector<Item>	craft_list;

	u32	craft(Item ingredient[4]) {
		u32 id = -1;

		
		if (id != -1) {
			return (id);
		}
		return (-1);
	}
	void	render_craft(double delta_time) {
		
	}
};
